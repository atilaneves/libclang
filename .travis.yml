language: d
sudo: false

matrix:
    include:
        - d: dmd-2.081.2
        - d: dmd-2.080.1
        - d: ldc-1.10.0

addons:
  apt:
    packages:
    - libclang-3.9-dev


script:
  - dub test --build=unittest-cov --compiler=${DC}

after_success:
 - bash <(curl -s https://codecov.io/bash)
